version: '3'

volumes:
  openrefine_data_vol:

services:
  openrefine:
    image: vimagick/openrefine
    volumes:
      - openrefine_data_vol:/data
    network_mode: host
        #- ./web.xml:/app/webapp/WEB-INF/web.xml
        #    ports:
        #      - "3333:3333"

  fast-reconcile:
    build: .
    network_mode: host
    #    ports:
    #      - "5000:5000"
    
